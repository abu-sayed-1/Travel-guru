import React from 'react';
// import { useParams } from 'react-router-dom';

const Room = () => {
    // const {numId} = useParams();
    // console.log(numId);
    return (
        <div>
            <p>this is Room</p>
        </div>
    );
};

export default Room;